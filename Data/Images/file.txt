 var imagesPath = Path.Combine(_env.WebRootPath, "images", "products", product.ProductId);

            if (!Directory.Exists(imagesPath))
                Directory.CreateDirectory(imagesPath);

            var filePath = Path.Combine(imagesPath, request.FileName);
            await File.WriteAllBytesAsync(filePath, imageBytes);

            product.ImageURLs.Add($"/images/products/{request.FileName}");

            await _productRepo.UpdateProductAsync(product);